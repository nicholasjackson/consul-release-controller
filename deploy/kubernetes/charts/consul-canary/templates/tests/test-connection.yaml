apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consul-canary.fullname" . }}-test-connection"
  labels:
    {{- include "consul-canary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "consul-canary.fullname" . }}:{{ .Values.webhook.port }}']
  restartPolicy: Never
